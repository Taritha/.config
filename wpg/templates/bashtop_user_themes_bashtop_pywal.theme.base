#Bashtop theme with default colors and black background
#by aristocratos

# Colors should be in 6 or 2 character hexadecimal or single spaced rgb decimal: "#RRGGBB", "#BW" or "0-255 0-255 0-255"
# example for white: "#FFFFFF", "#ff" or "255 255 255".

# All graphs and meters can be gradients
# For single color graphs leave "mid" and "end" variable empty.
# Use "start" and "end" variables for two color gradient
# Use "start", "mid" and "end" for three color gradient

# Main background, empty for terminal default, need to be empty if you want transparent background
theme[main_bg]="{background}"

# Main text color
theme[main_fg]="{foreground}"

# Title color for boxes
theme[title]="{color12}"

# Higlight color for keyboard shortcuts
theme[hi_fg]="{color4}"

# Background color of selected item in processes box
theme[selected_bg]="{background}" 

# Foreground color of selected item in processes box
theme[selected_fg]="{color14}"

# Color of inactive/disabled text
theme[inactive_fg]="{color8}"

# Misc colors for processes box including mini cpu graphs, details memory graph and details status text
theme[proc_misc]="{color5}"

# Cpu box outline color
theme[cpu_box]="{color9}"

# Memory/disks box outline color
theme[mem_box]="{color9}"

# Net up/down box outline color
theme[net_box]="{color9}"

# Processes box outline color
theme[proc_box]="{color9}"

# Box divider line and small boxes line color
theme[div_line]="{color9}"

# Temperature graph colors
theme[temp_start]="{color10}"
theme[temp_mid]="{color5}"
theme[temp_end]="{color12}"

# CPU graph colors
theme[cpu_start]="{color10}"
theme[cpu_mid]="{color5}"
theme[cpu_end]="{color12}"

# Mem/Disk free meter
theme[free_start]="{color10}"
theme[free_mid]="{color5}"
theme[free_end]="{color12}"

# Mem/Disk cached meter
theme[cached_start]="{color10}"
theme[cached_mid]="{color5}"
theme[cached_end]="{color12}"

# Mem/Disk available meter
theme[available_start]="{color10}"
theme[available_mid]="{color5}"
theme[available_end]="{color12}"

# Mem/Disk used meter
theme[used_start]="{color10}"
theme[used_mid]="{color5}"
theme[used_end]="{color12}"

# Download graph colors
theme[download_start]="{color8}"
theme[download_mid]="{color9}"
theme[download_end]="{color7}"

# Upload graph colors
theme[upload_start]="{color8}"
theme[upload_mid]="{color5}"
theme[upload_end]="{color12}"